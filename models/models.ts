export interface RouteConfig {
    path: string;         // Path for the route
    label: string;        // Label to show in the sidebar or navbar
    icon?: string;        // Optional icon for display
  }